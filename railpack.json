{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "steps": {
    "build": {
      "commands": [
        {
          "type": "exec",
          "command": "pnpm --filter @tailfire/api build"
        }
      ]
    }
  },
  "deploy": {
    "startCommand": "pnpm --filter @tailfire/api start"
  }
}
